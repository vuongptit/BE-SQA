# Cấu hình Nginx Reverse Proxy cho Be-Quiz-Sys
# File này nên được đặt tại /etc/nginx/sites-available/be-quiz-sys
# Sau đó tạo symlink: sudo ln -s /etc/nginx/sites-available/be-quiz-sys /etc/nginx/sites-enabled/

# Upstream server - Node.js application
upstream be_quiz_backend {
    # Có thể dùng nhiều server để load balancing
    server 127.0.0.1:5005;
    # server 127.0.0.1:5006;  # Uncomment nếu muốn load balancing
    keepalive 64;
}

# HTTP Server - Redirect to HTTPS (uncomment nếu có SSL)
# server {
#     listen 80;
#     server_name your-domain.com www.your-domain.com;
#     
#     # Redirect HTTP to HTTPS
#     return 301 https://$server_name$request_uri;
# }

# HTTP Server (nếu chưa có SSL)
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;  # Thay đổi domain của bạn
    
    # Logging
    access_log /var/log/nginx/be-quiz-access.log;
    error_log /var/log/nginx/be-quiz-error.log;

    # Client body size limit (cho upload files)
    client_max_body_size 50M;
    client_body_buffer_size 128k;

    # Timeouts
    proxy_connect_timeout 600;
    proxy_send_timeout 600;
    proxy_read_timeout 600;
    send_timeout 600;

    # Serve static files directly (uploads)
    location /uploads/ {
        alias /path/to/Be-Quiz-Sys/public/uploads/;  # Thay đổi đường dẫn thực tế
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # API routes - Proxy to Node.js
    location /api/ {
        proxy_pass http://be_quiz_backend;
        proxy_http_version 1.1;
        
        # Headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        proxy_set_header Connection "";
        
        # WebSocket support (nếu cần)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Buffering
        proxy_buffering off;
        proxy_request_buffering off;
        
        # CORS headers (nếu cần override)
        # add_header 'Access-Control-Allow-Origin' '*' always;
        # add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
        # add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type' always;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Root location - có thể redirect hoặc serve static files
    location / {
        proxy_pass http://be_quiz_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS Server (uncomment và cấu hình khi có SSL certificate)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com www.your-domain.com;
# 
#     # SSL Configuration
#     ssl_certificate /path/to/ssl/certificate.crt;
#     ssl_certificate_key /path/to/ssl/private.key;
#     
#     # SSL Security Settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # Security Headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
# 
#     # Logging
#     access_log /var/log/nginx/be-quiz-ssl-access.log;
#     error_log /var/log/nginx/be-quiz-ssl-error.log;
# 
#     # Client body size limit
#     client_max_body_size 50M;
#     client_body_buffer_size 128k;
# 
#     # Timeouts
#     proxy_connect_timeout 600;
#     proxy_send_timeout 600;
#     proxy_read_timeout 600;
#     send_timeout 600;
# 
#     # Serve static files directly
#     location /uploads/ {
#         alias /path/to/Be-Quiz-Sys/public/uploads/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
# 
#     # API routes
#     location /api/ {
#         proxy_pass http://be_quiz_backend;
#         proxy_http_version 1.1;
#         
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $server_name;
#         proxy_set_header Connection "";
#         
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         
#         proxy_buffering off;
#         proxy_request_buffering off;
#     }
# 
#     # Health check
#     location /health {
#         access_log off;
#         return 200 "healthy\n";
#         add_header Content-Type text/plain;
#     }
# 
#     # Root location
#     location / {
#         proxy_pass http://be_quiz_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }


